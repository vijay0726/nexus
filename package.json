{
  "name": "nexus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r --parallel build",
    "clean": "pnpm -r --parallel clean",
    "docs:dev": "pnpm --filter @nexus/docs dev",
    "docs:build": "pnpm --filter @nexus/docs build",
    "docs:serve": "pnpm --filter @nexus/docs serve"
  },
  "devDependencies": {
    "@types/node": "^22.13.8",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vue": "^3.0.0"
  },
  "dependencies": {
    "@nexus/components": "workspace:^1.0.0"
  }
}